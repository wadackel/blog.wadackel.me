machine:
  node:
    version: 8.2.0
  post:
    - curl -o- -L https://yarnpkg.com/install.sh | bash

dependencies:
  cache_directories:
    - "~/.cache/yarn"
  pre:
    - yarn --version
  override:
    - yarn install

deployment:
  production:
    branch: master
    commands:
      - ./deploy.sh
